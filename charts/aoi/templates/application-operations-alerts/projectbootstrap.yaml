{{- if .Values.alerting.createNamespace.enabled }}
---
apiVersion: project.tcs.trifork.com/v1alpha1
kind: ProjectBootstrap
metadata:
  name: application-operations-alerts
  namespace: netic-gitops-system
spec:
  namespace: application-operations-alerts
  config:
    ref: application-operations-alerts
    size: application-operations-alerts
  git:
    {{- toYaml .Values.alerts.projectBootstrap.git | nindent 4 }}
  metadata: {}
{{- end }}
